name: nineth-workflow
on: [push]
run-name: "Dosplay Context information"

jobs:
  showing-context:
    runs-on: ubuntu-latest

    steps:
      - name: Context and Expressions
        id: context-expressions
        run: |
          echo '${{ github.event.commits.committer }}'
